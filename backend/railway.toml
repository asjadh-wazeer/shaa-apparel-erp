[build]
builder = "NIXPACKS"
buildCommand = "npx prisma generate && npm run build"

[deploy]
startCommand = "sh -c 'node_modules/.bin/prisma db push --accept-data-loss --skip-generate && node dist/prisma/seed.js && node dist/src/main'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
